{% extends 'base.html.twig' %}

{% block title %}Admin - figure{% endblock %}{% block Masthead %}

    {% include ('layout/header-change-image.html.twig') %}

{% endblock %}



{% block body  %}

    <div class="container mt-4">
        <h1>Gérer les figures</h1>
        <table class="table table-striped">
            <thead>
            <tr>
                <th>Titre</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for trick in tricks %}
                <tr>
                    <td>{{ trick.title }}</td>
                    <td>
                        <a href="{{ path('trick.edit', {slug: trick.slug}) }}" class="btn btn-secondary">Modifier</a>
                        <a href="{{ path('trick.delete', {slug: trick.slug}) }}" class="btn btn-danger">Supprimer</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>


{% endblock %}
