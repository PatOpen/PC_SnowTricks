<div class="container">
    <div class="row">
        {{ form_start(form, {'attr': {'class': 'col-sm-12'}}) }}
        <div class="form-row align-items-center justify-content-center">
            {{ form_errors(form) }}
            <div class="col-sm-8">
                {{ form_row(form.content) }}

            </div>
            <div class="col-auto">
                <button class="btn btn-primary btn-lg text-uppercase" type="submit">Enregistrer</button>
            </div>
        </div>
        {{ form_end(form) }}
    </div><hr>
</div>